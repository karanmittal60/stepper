import React from 'react';

function ResearchOrderEntry(props) {

    return (
        <div className="App">
            <h1>ResearchOrderEntry</h1>
            {/*<button onClick={() => props.onClickNext(props.stepData)}>Next</button>*/}
        </div>
    );
}

export default ResearchOrderEntry;
